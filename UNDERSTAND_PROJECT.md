# 📚 เข้าใจ Project ตั้งแต่ต้น

## 🎯 โปรเจคนี้คืออะไร?

**LINE Bot ที่ช่วยเกษตรกรตรวจโรคพืชด้วย AI**

### สถานการณ์จริง:
```
เกษตรกร: "ทุเรียนของผมใบม้วน มีจุดสีเหลือง ไม่รู้เป็นอะไร"
         ↓ ถ่ายรูป ส่งผ่าน LINE
Bot:     "เป็นเพลี้ยไฟครับ แนะนำใช้ โมเดิน 50 หรือ อิมิดาโกลด์ 70"
```

---

## 📁 โครงสร้าง Folder (แบบละเอียด)

```
Chatbot-disease-ladda/
│
├── 📱 app/                          # โค้ดหลักของ Bot
│   ├── main.py                      # ❤️ หัวใจของระบบ (600+ บรรทัด)
│   ├── handlers.py                  # จัดการข้อความ (ไม่ได้ใช้)
│   └── agents/                      # Multi-agent system (ไม่ได้ใช้)
│
├── 📜 scripts/                      # สคริปต์ช่วยเหลือ
│   ├── setup_supabase.sql          # สร้างตารางใน Database
│   ├── import_csv_to_supabase.py   # นำเข้าข้อมูลผลิตภัณฑ์
│   ├── clear_products.py           # ลบข้อมูลทั้งหมด
│   ├── test_direct_search.py       # ทดสอบการค้นหา
│   └── preview_csv.py              # ดูข้อมูล CSV
│
├── 🧪 tests/                        # ไฟล์ทดสอบ
│   ├── test_supabase.py            # ทดสอบ Database
│   ├── test_rag.py                 # ทดสอบ RAG system
│   └── test_webhook.py             # ทดสอบ LINE webhook
│
├── 📖 docs/                         # เอกสารคู่มือ
│   ├── SUPABASE_SETUP.md           # ตั้งค่า Supabase
│   ├── MIGRATION_GUIDE.md          # ย้ายจาก Pinecone
│   ├── DEPLOYMENT_PRODUCTION.md    # Deploy จริง
│   └── ...
│
├── ⚙️ config/                       # ไฟล์ config (ไม่สำคัญ)
│
├── 📊 Data ICPL product for iDA.csv # ข้อมูลผลิตภัณฑ์ 43 รายการ
│
├── 🔧 requirements.txt              # Python packages ที่ต้องใช้
├── 🐳 Dockerfile                    # สำหรับ Deploy
├── 🔒 .env                          # API Keys (ห้าม push!)
├── 📝 README.md                     # เอกสารหลัก
└── 📋 CHANGELOG.md                  # บันทึกการเปลี่ยนแปลง
```

---

## 🔍 ไฟล์สำคัญที่ต้องรู้

### 1. **app/main.py** (ไฟล์หลัก - 600+ บรรทัด)

นี่คือหัวใจของระบบ มีทุกอย่างอยู่ในไฟล์เดียว:

```python
# โครงสร้างของ main.py

# ส่วนที่ 1: Import และ Setup (บรรทัด 1-100)
import FastAPI, OpenAI, Supabase, LINE API
load environment variables (.env)
initialize services

# ส่วนที่ 2: Models (บรรทัด 100-150)
class DiseaseDetectionResult  # ผลการตรวจโรค
class ProductRecommendation   # ข้อมูลผลิตภัณฑ์

# ส่วนที่ 3: Helper Functions (บรรทัด 150-250)
verify_line_signature()       # ตรวจสอบ LINE webhook
get_image_from_line()         # ดาวน์โหลดรูปจาก LINE

# ส่วนที่ 4: Core Functions (บรรทัด 250-500)
detect_disease()              # วิเคราะห์รูปด้วย OpenAI Vision
retrieve_product_recommendation()  # ค้นหาผลิตภัณฑ์
generate_final_response()     # สร้างข้อความตอบกลับ

# ส่วนที่ 5: API Endpoints (บรรทัด 500-650)
@app.get("/")                 # หน้าแรก
@app.get("/health")           # ตรวจสอบสถานะ
@app.post("/webhook")         # รับข้อความจาก LINE
```

---

### 2. **requirements.txt** (Python Packages)

```txt
fastapi==0.115.0              # Web framework
uvicorn[standard]==0.32.0     # Web server
supabase==2.3.4               # Database client
openai==1.54.0                # OpenAI API
httpx==0.27.2                 # HTTP client
python-dotenv==1.0.1          # Load .env file
pydantic==2.9.2               # Data validation
```

---

### 3. **.env** (API Keys - ห้าม push!)

```env
# LINE Bot
LINE_CHANNEL_ACCESS_TOKEN=xxx    # ส่งข้อความ
LINE_CHANNEL_SECRET=xxx          # ตรวจสอบ webhook

# OpenAI
OPENAI_API_KEY=xxx               # Vision + Embeddings

# Supabase
SUPABASE_URL=xxx                 # Database URL
SUPABASE_KEY=xxx                 # API Key
```

---

### 4. **Data ICPL product for iDA.csv** (ข้อมูลผลิตภัณฑ์)

```csv
ชื่อสินค้า,สารสำคัญ,ศัตรูพืชที่กำจัดได้,ใช้ได้กับพืช,วิธีใช้
โมเดิน 50,OMETHOATE 50%,เพลี้ยไฟ เพลี้ยจักจั่น,ทุกพืช,ฉีดพ่น 300cc/200L
อิมิดาโกลด์ 70,IMIDACLOPRID 70%,เพลี้ยกระโดด,ทุกพืช,ฉีดพ่น 50-100g/200L
...
```

**มี 43 ผลิตภัณฑ์** แบ่งเป็น:
- 🐛 Insecticide (ยากำจัดแมลง) - 20 รายการ
- 🍄 Fungicide (ยากำจัดเชื้อรา) - 15 รายการ
- 🌿 Herbicide (ยากำจัดวัชพืช) - 6 รายการ
- 🌱 PGR (ควบคุมการเจริญเติบโต) - 2 รายการ

---

## 🔄 Flow การทำงาน (Step by Step)

### ขั้นตอนที่ 1: เกษตรกรส่งรูป

```
User (LINE App)
    ↓ ส่งรูปภาพพืช
LINE Server
    ↓ ส่ง webhook
Your Server (app/main.py)
```

### ขั้นตอนที่ 2: รับและประมวลผล

```python
# ใน app/main.py

@app.post("/webhook")
async def webhook(request: Request):
    # 1. รับ event จาก LINE
    events = webhook_data.get("events", [])
    
    # 2. ตรวจสอบว่าเป็นรูปภาพ
    if message_type == "image":
        message_id = message.get("id")
        
        # 3. ดาวน์โหลดรูปจาก LINE
        image_bytes = await get_image_content_from_line(message_id)
        
        # 4. ส่งไป OpenAI Vision
        disease_result = await detect_disease(image_bytes)
        
        # 5. ค้นหาผลิตภัณฑ์
        recommendations = await retrieve_product_recommendation(disease_result)
        
        # 6. สร้างข้อความตอบกลับ
        final_message = await generate_final_response(disease_result, recommendations)
        
        # 7. ส่งกลับไปที่ LINE
        await reply_line(reply_token, final_message)
```

### ขั้นตอนที่ 3: OpenAI Vision วิเคราะห์

```python
async def detect_disease(image_bytes: bytes):
    # แปลงรูปเป็น base64
    image_base64 = base64.b64encode(image_bytes).decode('utf-8')
    
    # ส่งไป OpenAI
    response = openai_client.chat.completions.create(
        model="gpt-4o",
        messages=[{
            "role": "user",
            "content": [
                {"type": "text", "text": "วิเคราะห์โรคพืช..."},
                {"type": "image_url", "image_url": {"url": f"data:image/jpeg;base64,{image_base64}"}}
            ]
        }]
    )
    
    # ได้ผลลัพธ์
    return {
        "disease_name": "เพลี้ยไฟ",
        "pest_type": "ศัตรูพืช",
        "confidence": "สูง",
        "symptoms": "ใบม้วน มีจุดสีเหลือง",
        "severity": "ปานกลาง"
    }
```

### ขั้นตอนที่ 4: ค้นหาผลิตภัณฑ์

```python
async def retrieve_product_recommendation(disease_info):
    # ดึง keywords จากโรค
    keywords = ["เพลี้ยไฟ", "ศัตรูพืช", "แมลง"]
    
    # ค้นหาใน Supabase
    response = supabase.table('products').select('*').or_(
        f'target_pest.ilike.%เพลี้ยไฟ%,product_name.ilike.%เพลี้ยไฟ%'
    ).execute()
    
    # ได้ผลิตภัณฑ์ 5 รายการ
    return [
        {"product_name": "โมเดิน 50", "score": 0.85},
        {"product_name": "อิมิดาโกลด์ 70", "score": 0.80},
        ...
    ]
```

### ขั้นตอนที่ 5: สร้างข้อความตอบกลับ

```python
async def generate_final_response(disease_info, recommendations):
    message = f"""
ผลตรวจจากภาพ: {disease_info.disease_name}

ระดับความมั่นใจ: {disease_info.confidence}
ความรุนแรง: {disease_info.severity}

อาการที่เห็น: {disease_info.symptoms}

💊 สินค้าแนะนำ:

1. โมเดิน 50
   • สารสำคัญ: OMETHOATE 50%
   • ศัตรูพืช: เพลี้ยไฟ เพลี้ยจักจั่น
   • ใช้ได้กับพืช: ทุกพืช
   • วิธีใช้: ฉีดพ่น 300cc/200L
   • ความเกี่ยวข้อง: 85%

2. อิมิดาโกลด์ 70
   ...

📚 ดูรายละเอียดผลิตภัณฑ์ทั้งหมด:
🔗 https://www.icpladda.com/about/
"""
    return message
```

### ขั้นตอนที่ 6: ส่งกลับไปที่ LINE

```python
async def reply_line(reply_token: str, message: str):
    url = "https://api.line.me/v2/bot/message/reply"
    payload = {
        "replyToken": reply_token,
        "messages": [{"type": "text", "text": message}]
    }
    await httpx.post(url, json=payload, headers={...})
```

---

## 🗄️ Database (Supabase)

### ตาราง: products

```sql
CREATE TABLE products (
    id BIGSERIAL PRIMARY KEY,
    product_name TEXT,              -- ชื่อสินค้า
    active_ingredient TEXT,         -- สารสำคัญ
    target_pest TEXT,               -- ศัตรูพืชที่กำจัดได้
    applicable_crops TEXT,          -- ใช้ได้กับพืช
    how_to_use TEXT,               -- วิธีใช้
    product_group TEXT,            -- กลุ่มสาร (Insecticide/Fungicide)
    formulation TEXT,              -- รูปแบบ (EC/WP/SC)
    usage_rate TEXT,               -- อัตราการใช้
    metadata JSONB,                -- ข้อมูลเพิ่มเติม
    embedding vector(1536),        -- Vector (ไม่ได้ใช้)
    created_at TIMESTAMP
);
```

### ข้อมูลตัวอย่าง:

```
id: 1
product_name: โมเดิน 50
active_ingredient: OMETHOATE 50% W/V SL
target_pest: เพลี้ยไฟ เพลี้ยจักจั่นฝอย เพลี้ยไก่แจ้
applicable_crops: ปลอดภัยใช้ได้ทุกพืช โดยเฉพาะทุเรียน
how_to_use: ผสมน้ำฉีดพ่นบริเวณที่ใบให้ทั่วทรงพุ่ม
usage_rate: 300 ซีซีต่อ 200 ลิตร
```

---

## 🔑 API Keys ที่ต้องมี

### 1. LINE Developers (ฟรี)

**ขั้นตอน:**
1. ไปที่ https://developers.line.biz/console/
2. สร้าง Provider
3. สร้าง Messaging API channel
4. Copy:
   - Channel Access Token
   - Channel Secret

### 2. OpenAI (มีค่าใช้จ่าย)

**ขั้นตอน:**
1. ไปที่ https://platform.openai.com/api-keys
2. สร้าง API key
3. เติมเงิน $5-10
4. Copy API key

**ค่าใช้จ่าย:**
- GPT-4o Vision: ~$0.01-0.03 ต่อรูป
- Embeddings: ~$0.0001 ต่อ query

### 3. Supabase (ฟรี)

**ขั้นตอน:**
1. ไปที่ https://supabase.com
2. สร้าง project
3. รัน SQL: `scripts/setup_supabase.sql`
4. Import ข้อมูล: `python scripts/import_csv_to_supabase.py`
5. Copy:
   - Project URL
   - Anon Key

---

## 🚀 วิธีใช้งาน (ทีละขั้นตอน)

### ขั้นตอนที่ 1: ติดตั้ง Dependencies

```bash
pip install -r requirements.txt
```

### ขั้นตอนที่ 2: ตั้งค่า .env

```bash
# Copy ตัวอย่าง
cp .env.example .env

# แก้ไข .env ใส่ API keys
LINE_CHANNEL_ACCESS_TOKEN=xxx
LINE_CHANNEL_SECRET=xxx
OPENAI_API_KEY=xxx
SUPABASE_URL=xxx
SUPABASE_KEY=xxx
```

### ขั้นตอนที่ 3: Setup Supabase

```bash
# 1. ไปที่ Supabase SQL Editor
# 2. Copy โค้ดจาก scripts/setup_supabase.sql
# 3. Run

# 4. Import ข้อมูล
python scripts/import_csv_to_supabase.py
```

### ขั้นตอนที่ 4: ทดสอบ

```bash
# ทดสอบ Supabase
python tests/test_supabase.py

# รัน Server
python app/main.py
# Server: http://localhost:8000

# ทดสอบ Health
curl http://localhost:8000/health
```

### ขั้นตอนที่ 5: ตั้งค่า LINE Webhook

```bash
# 1. ใช้ ngrok expose local server
ngrok http 8000

# 2. Copy URL: https://xxx.ngrok.io

# 3. ไปที่ LINE Console
# 4. Webhook URL: https://xxx.ngrok.io/webhook
# 5. Verify

# 6. ทดสอบส่งรูปผ่าน LINE
```

---

## 📊 ตัวอย่างการใช้งานจริง

### Case 1: เพลี้ยไฟในทุเรียน

```
Input: รูปภาพใบทุเรียนที่มีเพลี้ยไฟ

Output:
ผลตรวจจากภาพ: เพลี้ยไฟ

ระดับความมั่นใจ: สูง
ความรุนแรง: ปานกลาง

อาการที่เห็น: ใบม้วน มีจุดสีเหลือง

💊 สินค้าแนะนำ:

1. โมเดิน 50
   • สารสำคัญ: OMETHOATE 50%
   • ศัตรูพืช: เพลี้ยไฟ เพลี้ยจักจั่น
   • ใช้ได้กับพืช: ทุกพืช โดยเฉพาะทุเรียน
   • วิธีใช้: ฉีดพ่น 300cc/200L
   • ความเกี่ยวข้อง: 85%

2. อิมิดาโกลด์ 70
   ...

📚 ดูรายละเอียด: https://www.icpladda.com/about/
```

### Case 2: แอนแทรคโนสในมะม่วง

```
Input: รูปภาพใบมะม่วงที่มีจุดสีน้ำตาล

Output:
ผลตรวจจากภาพ: แอนแทรคโนส

ระดับความมั่นใจ: สูง
ความรุนแรง: รุนแรง

อาการที่เห็น: จุดสีน้ำตาลบนใบและผล

💊 สินค้าแนะนำ:

1. เบนซาน่า เอฟ
   • สารสำคัญ: CARBENDAZIM 50%
   • ศัตรูพืช: แอนแทรคโนส ใบไหม้
   • ใช้ได้กับพืช: ทุกพืช
   ...
```

---

## 🐛 Troubleshooting

### ปัญหา 1: Server ไม่ทำงาน

```bash
# ตรวจสอบ Python version
python --version  # ต้อง 3.11+

# ตรวจสอบ packages
pip list

# ดู error logs
python app/main.py
```

### ปัญหา 2: LINE Webhook Failed

```bash
# ตรวจสอบ server
curl http://localhost:8000/health

# ตรวจสอบ ngrok
ngrok http 8000

# ตรวจสอบ .env
cat .env | grep LINE
```

### ปัญหา 3: OpenAI Error

```bash
# ตรวจสอบ API key
curl https://api.openai.com/v1/models \
  -H "Authorization: Bearer $OPENAI_API_KEY"

# ตรวจสอบ billing
https://platform.openai.com/account/billing
```

### ปัญหา 4: Supabase Connection Failed

```bash
# ทดสอบ connection
python tests/test_supabase.py

# ตรวจสอบ .env
cat .env | grep SUPABASE
```

---

## 💰 ค่าใช้จ่าย

### ต่อเดือน (100 users, 10 รูป/วัน)

| Service | ฟรี | Paid |
|---------|-----|------|
| LINE | ✅ ฟรี | ฟรี |
| Supabase | ✅ ฟรี (500MB) | $25/mo |
| OpenAI | ❌ | ~$30-50/mo |
| Cloud Run | ✅ ฟรี (2M req) | ~$5/mo |
| **รวม** | | **~$35-60/mo** |

---

## 📚 เอกสารเพิ่มเติม

- **PROJECT_OVERVIEW.md** - ภาพรวมโปรเจค
- **README.md** - เอกสารหลัก
- **ARCHITECTURE.md** - สถาปัตยกรรม
- **docs/SUPABASE_SETUP.md** - ตั้งค่า Supabase
- **docs/DEPLOYMENT_PRODUCTION.md** - Deploy production
- **QUICK_DEPLOY.md** - Deploy แบบเร็ว

---

## ✅ สรุป

**โปรเจคนี้คือ:**
- 🤖 LINE Bot ตรวจโรคพืชด้วย AI
- 📸 ใช้ OpenAI Vision วิเคราะห์รูปภาพ
- 💾 เก็บข้อมูลใน Supabase (43 ผลิตภัณฑ์)
- 🎯 แนะนำผลิตภัณฑ์ที่เหมาะสม
- 🇹🇭 รองรับภาษาไทย 100%
- ⚡ พร้อม deploy production

**ไฟล์สำคัญ:**
1. `app/main.py` - โค้ดหลัก (600+ บรรทัด)
2. `.env` - API keys
3. `Data ICPL product for iDA.csv` - ข้อมูลผลิตภัณฑ์
4. `requirements.txt` - Python packages

**ค่าใช้จ่าย:** ~$35-60/เดือน (ส่วนใหญ่เป็น OpenAI)

---

เข้าใจแล้วใช่ไหมครับ? มีอะไรอยากถามเพิ่มไหม? 😊
