# FastAPI and server
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2

# HTTP client (compatible with supabase)
httpx>=0.24,<0.26

# Google Gemini AI (compatible with Python 3.13)
google-generativeai==0.8.3

# Supabase client
supabase==2.3.4

# OpenAI for embeddings
openai==1.54.0

# Note: LightRAG has compatibility issues
# Using Supabase + keyword search instead (works better with Thai)

# Image processing
Pillow==11.0.0

# Environment variables
python-dotenv==1.0.1

# Utilities
python-multipart==0.0.12

# Additional dependencies
requests==2.32.3

# ไม่ต้องใช้ pandas - ใช้ built-in csv module แทน
# ไม่ต้องใช้ PDF libraries - ใช้ CSV เท่านั้น
