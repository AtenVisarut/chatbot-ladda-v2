# Webhook Payload Examples

Complete examples of LINE webhook payloads and system responses.

## 1. Text Message Event

### Incoming Webhook from LINE

```json
{
  "destination": "xxxxxxxxxx",
  "events": [
    {
      "type": "message",
      "message": {
        "type": "text",
        "id": "123456789",
        "text": "สวัสดี"
      },
      "timestamp": 1462629479859,
      "source": {
        "type": "user",
        "userId": "U4af4980629..."
      },
      "replyToken": "nHuyWiB7yP5Zw52FIkcQobQuGDXCTA",
      "mode": "active"
    }
  ]
}
```

### Bot Response to LINE

```json
{
  "replyToken": "nHuyWiB7yP5Zw52FIkcQobQuGDXCTA",
  "messages": [
    {
      "type": "text",
      "text": "สวัสดีค่ะ! 🌱\n\nฉันคือผู้ช่วยตรวจสอบโรคพืชด้วย AI..."
    }
  ]
}
```

## 2. Image Message Event (Main Flow)

### Incoming Webhook from LINE

```json
{
  "destination": "xxxxxxxxxx",
  "events": [
    {
      "type": "message",
      "message": {
        "type": "image",
        "id": "325708",
        "contentProvider": {
          "type": "line"
        }
      },
      "timestamp": 1462629479859,
      "source": {
        "type": "user",
        "userId": "U4af4980629..."
      },
      "replyToken": "nHuyWiB7yP5Zw52FIkcQobQuGDXCTA",
      "mode": "active"
    }
  ]
}
```

### Step 1: Download Image from LINE

**Request:**
```http
GET https://api-data.line.me/v2/bot/message/325708/content
Authorization: Bearer {LINE_CHANNEL_ACCESS_TOKEN}
```

**Response:**
```
Binary image data (JPEG/PNG)
```

### Step 2: Gemini Vision Analysis

**Request to Gemini:**
```python
{
  "prompt": "วิเคราะห์รูปภาพพืชนี้และตรวจหาโรคพืช...",
  "image": <binary_data>
}
```

**Gemini Response:**
```json
{
  "disease_name": "โรคใบจุด",
  "confidence": "สูง",
  "symptoms": "พบจุดสีน้ำตาลขนาดเล็กกระจายทั่วใบ มีขอบสีเหลือง",
  "severity": "ปานกลาง",
  "description": "โรคใบจุดเกิดจากเชื้อรา มักพบในช่วงฤดูฝน"
}
```

### Step 3: Pinecone RAG Query

**Query to Pinecone:**
```python
{
  "query_text": "โรคใบจุด พบจุดสีน้ำตาลขนาดเล็กกระจายทั่วใบ ปานกลาง",
  "top_k": 5,
  "filter": {"category": "plant-treatment"}
}
```

**Pinecone Response:**
```json
{
  "matches": [
    {
      "id": "prod-001",
      "score": 0.95,
      "metadata": {
        "product_name": "ปุ๋ยอินทรีย์ชีวภาพ Premium",
        "description": "ปุ๋ยอินทรีย์คุณภาพสูง เสริมสร้างภูมิคุ้มกันพืช",
        "usage": "ใช้ 2-3 ครั้งต่อเดือน ผสมน้ำตามอัตราส่วน 1:100"
      }
    },
    {
      "id": "prod-002",
      "score": 0.92,
      "metadata": {
        "product_name": "สารป้องกันกำจัดโรคพืช Bio-Safe",
        "description": "สารชีวภาพป้องกันโรคพืช ปลอดภัยต่อสิ่งแวดล้อม",
        "usage": "พ่นทุก 7-10 วัน ในช่วงเช้าหรือเย็น"
      }
    }
  ]
}
```

### Step 4: Generate Final Response with LLM

**Prompt to Gemini:**
```
คุณเป็นผู้เชี่ยวชาญด้านโรคพืช...

ผลการวิเคราะห์:
- โรคที่พบ: โรคใบจุด
- ความมั่นใจ: สูง
- อาการ: พบจุดสีน้ำตาลขนาดเล็กกระจายทั่วใบ
- ความรุนแรง: ปานกลาง

ผลิตภัณฑ์แนะนำ:
- ปุ๋ยอินทรีย์ชีวภาพ Premium
- สารป้องกันกำจัดโรคพืช Bio-Safe
```

**LLM Response:**
```
🔍 ผลการตรวจสอบโรคพืช

จากการวิเคราะห์รูปภาพ พบว่าพืชของคุณเป็น "โรคใบจุด" ค่ะ

📋 อาการที่พบ:
พบจุดสีน้ำตาลขนาดเล็กกระจายทั่วใบ มีขอบสีเหลือง 
ความรุนแรงอยู่ในระดับปานกลาง

💊 ผลิตภัณฑ์แนะนำ:

🌿 ปุ๋ยอินทรีย์ชีวภาพ Premium
ปุ๋ยอินทรีย์คุณภาพสูง เสริมสร้างภูมิคุ้มกันพืช
💡 วิธีใช้: ใช้ 2-3 ครั้งต่อเดือน ผสมน้ำตามอัตราส่วน 1:100

🌿 สารป้องกันกำจัดโรคพืช Bio-Safe
สารชีวภาพป้องกันโรคพืช ปลอดภัยต่อสิ่งแวดล้อม
💡 วิธีใช้: พ่นทุก 7-10 วัน ในช่วงเช้าหรือเย็น

📌 คำแนะนำเพิ่มเติม:
- ตัดใบที่เป็นโรคทิ้ง
- รักษาความสะอาดบริเวณแปลงปลูก
- หลีกเลี่ยงการรดน้ำโดนใบ

หากมีคำถามเพิ่มเติม สามารถส่งรูปภาพมาให้เราช่วยวิเคราะห์ได้เสมอค่ะ 😊
```

### Step 5: Reply to LINE User

**Request to LINE:**
```http
POST https://api.line.me/v2/bot/message/reply
Authorization: Bearer {LINE_CHANNEL_ACCESS_TOKEN}
Content-Type: application/json

{
  "replyToken": "nHuyWiB7yP5Zw52FIkcQobQuGDXCTA",
  "messages": [
    {
      "type": "text",
      "text": "🔍 ผลการตรวจสอบโรคพืช\n\n..."
    }
  ]
}
```

**LINE Response:**
```json
{
  "status": 200,
  "message": "ok"
}
```

## 3. Complete Flow Diagram

```
User sends image
    ↓
LINE Platform
    ↓
POST /webhook (Your Server)
    ↓
[1] Download image from LINE
    ↓
[2] Gemini Vision Analysis
    → Disease: "โรคใบจุด"
    → Confidence: "สูง"
    → Symptoms: "..."
    ↓
[3] Pinecone RAG Query
    → Query: "โรคใบจุด ..."
    → Results: [Product 1, Product 2, ...]
    ↓
[4] Generate Final Response (LLM)
    → Combine disease info + products
    → Format in Thai
    ↓
[5] Reply to LINE
    ↓
LINE Platform
    ↓
User receives response
```

## 4. Error Scenarios

### Scenario A: Invalid Image

**Gemini Response:**
```json
{
  "disease_name": "ไม่สามารถวิเคราะห์ได้",
  "confidence": "ต่ำ",
  "symptoms": "รูปภาพไม่ชัดเจนหรือไม่ใช่รูปพืช",
  "severity": "ไม่ทราบ"
}
```

**Bot Reply:**
```
ขออภัยค่ะ ไม่สามารถวิเคราะห์รูปภาพได้ชัดเจน

กรุณาถ่ายรูปใหม่โดย:
- ถ่ายในที่ที่มีแสงสว่างเพียงพอ
- ถ่ายใกล้ส่วนที่เป็นโรค
- ให้รูปภาพชัดเจน ไม่เบลอ

ลองส่งรูปภาพใหม่มาอีกครั้งนะคะ 😊
```

### Scenario B: No Disease Found

**Gemini Response:**
```json
{
  "disease_name": "ไม่พบโรค",
  "confidence": "สูง",
  "symptoms": "พืชมีสุขภาพดี ใบเขียวสด",
  "severity": "ไม่มี"
}
```

**Bot Reply:**
```
🎉 ข่าวดี!

จากการตรวจสอบ พืชของคุณมีสุขภาพดีค่ะ ไม่พบโรคหรือความผิดปกติ

💡 คำแนะนำการดูแล:
- รดน้ำสม่ำเสมอ
- ใส่ปุ๋ยตามกำหนด
- ตรวจสอบพืชเป็นประจำ

หากสังเกตเห็นอาการผิดปกติ สามารถส่งรูปมาให้เราช่วยดูได้เสมอค่ะ 🌱
```

### Scenario C: Service Error

**Bot Reply:**
```
ขออภัยค่ะ เกิดข้อผิดพลาดในการวิเคราะห์รูปภาพ

กรุณาลองใหม่อีกครั้งในอีกสักครู่ค่ะ 🙏

หากปัญหายังคงอยู่ กรุณาติดต่อทีมสนับสนุน
```

## 5. Testing Payloads

### Test with curl

```bash
# Test health endpoint
curl http://localhost:8000/health

# Test webhook with text message
curl -X POST http://localhost:8000/webhook \
  -H "Content-Type: application/json" \
  -H "X-Line-Signature: test-signature" \
  -d '{
    "events": [{
      "type": "message",
      "message": {"type": "text", "text": "สวัสดี"},
      "replyToken": "test-token",
      "source": {"type": "user", "userId": "test-user"}
    }]
  }'
```

### Test with Python

```python
import requests

# Test health
response = requests.get("http://localhost:8000/health")
print(response.json())

# Test webhook
payload = {
    "events": [{
        "type": "message",
        "message": {"type": "text", "text": "สวัสดี"},
        "replyToken": "test-token",
        "source": {"type": "user", "userId": "test-user"}
    }]
}
response = requests.post(
    "http://localhost:8000/webhook",
    json=payload,
    headers={"X-Line-Signature": "test"}
)
print(response.json())
```

---

For more examples, see test_webhook.py
