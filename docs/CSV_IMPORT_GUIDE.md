# 📊 คู่มือนำเข้าข้อมูลผลิตภัณฑ์จาก CSV

## 🎯 ภาพรวม

คู่มือนี้จะแนะนำวิธีนำเข้าข้อมูลผลิตภัณฑ์จากไฟล์ CSV ของคุณไปยัง Pinecone เพื่อให้ LINE Bot แนะนำผลิตภัณฑ์ที่ถูกต้อง

## 📋 ขั้นตอนการนำเข้า

### ขั้นตอนที่ 1: เตรียมไฟล์ CSV

ไฟล์ CSV ควรมี columns เหล่านี้ (ปรับตามข้อมูลจริงของคุณ):

```csv
ชื่อผลิตภัณฑ์,คำอธิบาย,ประเภท,โรคที่รักษา,วิธีใช้,ราคา
ปุ๋ยอินทรีย์ชีวภาพ,ปุ๋ยอินทรีย์คุณภาพสูง,ปุ๋ย,โรคใบจุด โรคราน้ำค้าง,ใช้ 2-3 ครั้งต่อเดือน,350
```

**Columns ที่แนะนำ:**
- ชื่อผลิตภัณฑ์ (จำเป็น)
- คำอธิบาย/สรรพคุณ
- ประเภท (ปุ๋ย/ยา/อื่นๆ)
- โรคที่รักษา/เหมาะสำหรับ
- วิธีใช้
- ราคา
- ขนาดบรรจุ
- หมายเหตุ

### ขั้นตอนที่ 2: ดูโครงสร้าง CSV

```bash
python scripts/preview_csv.py
```

ระบุ path ไฟล์ CSV:
```
Path: C:/Users/YourName/Downloads/products.csv
```

หรือ:
```
Path: data/products.csv
```

**ผลลัพธ์:**
```
📋 Columns ในไฟล์ CSV
1. ชื่อผลิตภัณฑ์
2. คำอธิบาย
3. ประเภท
...

📊 ตัวอย่างข้อมูล (5 แถวแรก)
--- แถวที่ 1 ---
ชื่อผลิตภัณฑ์: ปุ๋ยอินทรีย์ชีวภาพ Premium
คำอธิบาย: ปุ๋ยอินทรีย์คุณภาพสูง...
```

### ขั้นตอนที่ 3: นำเข้าข้อมูล

```bash
python scripts/import_csv_to_pinecone.py
```

ระบุ path ไฟล์ CSV เดียวกัน:
```
Path ไฟล์ CSV: C:/Users/YourName/Downloads/products.csv
```

สคริปต์จะ:
1. ✅ อ่านข้อมูลจาก CSV
2. ✅ แสดงตัวอย่างข้อมูล
3. ✅ ขอยืนยันก่อนอัพโหลด
4. ✅ สร้าง embeddings ด้วย OpenAI
5. ✅ อัพโหลดไปยัง Pinecone

**ผลลัพธ์:**
```
📦 กำลังเตรียมข้อมูล...
  1/50: ปุ๋ยอินทรีย์ชีวภาพ Premium...
  2/50: สารป้องกันกำจัดโรคพืช Bio-Safe...
  ...
✅ เตรียมข้อมูลเสร็จสิ้น: 50 รายการ

⬆️  กำลังอัพโหลดไปยัง Pinecone...
  อัพโหลด batch 1: 50 รายการ
✅ อัพโหลดสำเร็จ: 50 รายการ
```

### ขั้นตอนที่ 4: Restart Server

```bash
# กด Ctrl+C เพื่อหยุด server
python app/main.py
```

### ขั้นตอนที่ 5: ทดสอบ

1. ส่งรูปพืชผ่าน LINE
2. Bot จะวิเคราะห์โรค
3. Bot จะแนะนำผลิตภัณฑ์จากข้อมูลของคุณ

---

## 🔧 การปรับแต่ง

### ปรับ Columns ที่ใช้สำหรับ Search

แก้ไขใน `scripts/import_csv_to_pinecone.py`:

```python
# บรรทัดที่ ~80
searchable_text = " ".join([
    str(product.get('ชื่อผลิตภัณฑ์', '')),
    str(product.get('คำอธิบาย', '')),
    str(product.get('โรคที่รักษา', '')),
    # เพิ่ม columns อื่นๆ ที่ต้องการ
])
```

### เปลี่ยน Embedding Model

แก้ไขใน `scripts/import_csv_to_pinecone.py`:

```python
# บรรทัดที่ ~50
response = openai_client.embeddings.create(
    model="text-embedding-3-small",  # เปลี่ยนเป็น model อื่น
    input=text
)
```

**Models ที่ใช้ได้:**
- `text-embedding-3-small` (1536 dim) - แนะนำ, ถูก
- `text-embedding-3-large` (3072 dim) - แม่นยำกว่า, แพงกว่า
- `text-embedding-ada-002` (1536 dim) - รุ่นเก่า

---

## 📊 ตัวอย่าง CSV Format

### Format 1: พื้นฐาน

```csv
ชื่อผลิตภัณฑ์,คำอธิบาย,วิธีใช้,ราคา
ปุ๋ยอินทรีย์ A,ปุ๋ยอินทรีย์คุณภาพสูง,ใช้ 2-3 ครั้งต่อเดือน,350
ยาฆ่าแมลง B,ยาฆ่าแมลงชีวภาพ,พ่นทุก 7 วัน,280
```

### Format 2: ละเอียด

```csv
รหัสสินค้า,ชื่อผลิตภัณฑ์,ประเภท,คำอธิบาย,สรรพคุณ,โรคที่รักษา,วิธีใช้,ขนาดบรรจุ,ราคา,หมายเหตุ
P001,ปุ๋ยอินทรีย์ชีวภาพ Premium,ปุ๋ย,ปุ๋ยอินทรีย์คุณภาพสูง,เสริมสร้างภูมิคุ้มกัน,โรคใบจุด โรคราน้ำค้าง,ใช้ 2-3 ครั้งต่อเดือน,1 ลิตร,350,ปลอดภัย
P002,สารป้องกันโรค Bio-Safe,ยา,สารชีวภาพป้องกันโรค,ป้องกันโรคพืช,โรคราแป้ง โรคใบด่าง,พ่นทุก 7-10 วัน,250 มล.,280,ไม่เป็นพิษ
```

---

## ⚙️ การอัพเดทข้อมูล

### อัพเดทข้อมูลทั้งหมด

```bash
# รันสคริปต์ใหม่ ข้อมูลเก่าจะถูกเขียนทับ
python scripts/import_csv_to_pinecone.py
```

### เพิ่มข้อมูลใหม่

```bash
# ใช้ไฟล์ CSV ที่มีเฉพาะข้อมูลใหม่
python scripts/import_csv_to_pinecone.py
```

### ลบข้อมูลทั้งหมด

```bash
python scripts/setup_pinecone.py
# เลือก "yes" เมื่อถามว่าจะลบ index เดิม
```

---

## 🔍 การทดสอบ

### ทดสอบว่าข้อมูลอัพโหลดสำเร็จ

```python
from pinecone import Pinecone
import os

pc = Pinecone(api_key=os.getenv("PINECONE_API_KEY"))
index = pc.Index("plant-products")

# ดูสถิติ
stats = index.describe_index_stats()
print(f"Total vectors: {stats.total_vector_count}")

# Query ทดสอบ
results = index.query(
    vector=[0.0] * 1536,
    top_k=5,
    include_metadata=True
)

for match in results.matches:
    print(match.metadata.get('ชื่อผลิตภัณฑ์'))
```

---

## 💰 ค่าใช้จ่าย

### OpenAI Embeddings

- **text-embedding-3-small**: $0.02 / 1M tokens
- **text-embedding-3-large**: $0.13 / 1M tokens

**ประมาณการ:**
- 100 ผลิตภัณฑ์ ≈ 10,000 tokens ≈ $0.0002
- 1,000 ผลิตภัณฑ์ ≈ 100,000 tokens ≈ $0.002
- 10,000 ผลิตภัณฑ์ ≈ 1M tokens ≈ $0.02

### Pinecone

- **Serverless**: ฟรีสำหรับ usage น้อย
- **Pod-based**: เริ่มต้น ~$70/เดือน

---

## ❓ FAQ

### Q: ไฟล์ CSV ต้องเป็นภาษาไทยได้ไหม?
A: ได้! สคริปต์รองรับ UTF-8

### Q: มีข้อมูลกี่รายการถึงจะดี?
A: แนะนำอย่างน้อย 20-50 รายการ ยิ่งมากยิ่งดี

### Q: ต้อง re-import ทุกครั้งที่แก้ไขข้อมูลไหม?
A: ใช่ หรือสามารถเขียนสคริปต์ update เฉพาะรายการที่เปลี่ยน

### Q: ถ้า CSV มี columns เยอะมาก?
A: ไม่เป็นไร สคริปต์จะเก็บทุก column ใน metadata

### Q: ต้องใช้ OpenAI ไหม?
A: ใช่ สำหรับสร้าง embeddings (หรือใช้ model อื่นแทนได้)

---

## 🆘 Troubleshooting

### Error: "File not found"
```bash
# ตรวจสอบ path
python scripts/preview_csv.py
```

### Error: "Encoding error"
```bash
# บันทึก CSV เป็น UTF-8
# ใน Excel: Save As → CSV UTF-8
```

### Error: "OpenAI API error"
```bash
# ตรวจสอบ API key
echo $OPENAI_API_KEY  # Linux/Mac
echo %OPENAI_API_KEY%  # Windows
```

### Error: "Pinecone dimension mismatch"
```bash
# ลบ index แล้วสร้างใหม่
python scripts/setup_pinecone.py
```

---

**เสร็จแล้ว!** Bot จะแนะนำผลิตภัณฑ์จากข้อมูลของคุณ 🎉
