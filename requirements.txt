# FastAPI and server
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2

# HTTP client (compatible with all services)
httpx>=0.28.1

# Google Gemini AI (compatible with Python 3.13)
google-generativeai==0.8.3

# Supabase client (updated for httpx compatibility)
supabase>=2.10.0

# OpenAI for embeddings and LLM
openai>=1.54.0

# Sentence Transformers for E5 embeddings
sentence-transformers>=2.2.0

# Image processing
Pillow==11.0.0

# Environment variables
python-dotenv==1.0.1

# Utilities
python-multipart==0.0.12

# Rate limiting
slowapi==0.1.9

# Additional dependencies
requests==2.32.3

# ไม่ต้องใช้ pandas - ใช้ built-in csv module แทน
# ไม่ต้องใช้ PDF libraries - ใช้ CSV เท่านั้น
