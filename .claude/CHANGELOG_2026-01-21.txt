# Changelog - 21 มกราคม 2026

## สรุปการเปลี่ยนแปลงวันนี้

### 1. แก้ไขระบบแนะนำยาฆ่าแมลงสำหรับโรคที่มีพาหะ

**ไฟล์:** `app/main.py`

**ปัญหา:**
โรคใบด่างมันสำปะหลัง (Cassava Mosaic Disease) ไม่ได้รับการแนะนำยาฆ่าแมลง ทั้งที่:
- มีพาหะ: แมลงหวี่ขาว (Whitefly - Bemisia tabaci)
- มียาในฐานข้อมูล products ที่รักษาได้

**สาเหตุ:**
ใน `app/main.py:984` มี skip keyword "ใบด่าง" ซึ่งทำให้ระบบข้ามการแนะนำสินค้าทันที
โดยไม่ได้ตรวจสอบว่าโรคนี้มีพาหะหรือไม่

Log ที่เห็น:
```
⏭️ Skipping product recommendation - matched skip keyword: ใบด่าง
```

**การแก้ไข:**
1. Import `get_search_query_for_disease` จาก `product_recommendation.py`
2. แก้ไข logic ให้ตรวจสอบว่าโรคมีพาหะหรือไม่ก่อน skip

```python
# ก่อนแก้ไข (line 999-1003)
for kw in skip_keywords:
    if kw.lower() in disease_name_lower:
        should_recommend = False  # ข้ามทันที
        break

# หลังแก้ไข
for kw in skip_keywords:
    if kw.lower() in disease_name_lower:
        # ตรวจสอบว่าโรคนี้มีแมลงพาหะหรือไม่
        _, pest_name, _ = get_search_query_for_disease(detection_result.disease_name)
        if pest_name:
            # โรคนี้มีพาหะ → ยังแนะนำยาฆ่าแมลงได้
            logger.info(f"🐛 โรคมีพาหะ '{pest_name}' → ยังแนะนำยาฆ่าแมลงได้")
        else:
            should_recommend = False  # ไม่มีพาหะ → ข้ามตามเดิม
        break
```

**ผลลัพธ์:**
- โรคที่มีพาหะ (viral diseases with vector) จะได้รับการแนะนำยาฆ่าแมลง
- โรคที่ไม่มีพาหะและ match skip keyword จะยังคง skip ตามเดิม

**Commit:** `3dbaae6`

---

### 2. รายการโรคที่มีพาหะ (VECTOR_DISEASES)

อ้างอิงจาก `app/services/product_recommendation.py`:

| โรค | พาหะ |
|-----|------|
| โรคใบด่างมันสำปะหลัง | แมลงหวี่ขาว |
| โรคพุ่มแจ้มันสำปะหลัง | เพลี้ยจักจั่น |
| โรคจู๋ข้าว | เพลี้ยกระโดดสีน้ำตาล |
| โรคหูด/โรคใบสีส้ม | เพลี้ยจักจั่นเขียว |
| โรคใบหงิก | เพลี้ยกระโดดสีน้ำตาล |
| โรคข้าวโพดแคระ | เพลี้ยจักจั่นข้าวโพด |
| โรคใบลายข้าวโพด | เพลี้ยกระโดดข้าวโพด |
| โรคใบด่างข้าวโพด | เพลี้ยอ่อน |

---

### 3. Skip Keywords ที่ยังใช้งาน

```python
skip_keywords = [
    "ไม่พบ", "ไม่ทราบ", "ปกติ", "ไม่ชัดเจน",
    "ขาดธาตุ", "ขาดไนโตรเจน", "ขาดฟอสฟอรัส", "ขาดโพแทสเซียม",
    "ขาดแมกนีเซียม", "ขาดเหล็ก", "ขาดแคลเซียม", "ขาดโบรอน",
    "Deficiency", "deficiency",
    "ใบเหลือง", "ใบซีด", "ใบด่าง",  # ← ตอนนี้จะตรวจสอบพาหะก่อน skip
    "สุขภาพดี", "healthy", "Healthy",
    "Technical Error", "ไม่สามารถระบุได้", "Error", "error",
    "ไม่ใช่ภาพ", "ไม่ใช่รูป", "Not Found"
]
```

---

## Commits วันนี้

| Commit | รายละเอียด |
|--------|------------|
| `3dbaae6` | fix: แก้ไขระบบแนะนำยาฆ่าแมลงสำหรับโรคที่มีพาหะ |

---

## ไฟล์ที่แก้ไข

1. `app/main.py` - เพิ่ม import และแก้ไข skip keyword logic

---

## หมายเหตุสำคัญ

### Logic การแนะนำสินค้า (Updated)

```
1. ตรวจสอบ confidence < 10% → skip
2. ตรวจสอบ skip keywords:
   - ถ้า match → ตรวจสอบว่าโรคมีพาหะ
     - ถ้ามีพาหะ → แนะนำยาฆ่าแมลง ✓
     - ถ้าไม่มีพาหะ → skip ✗
   - ถ้าไม่ match → แนะนำสินค้าตามปกติ ✓
3. เรียก retrieve_products_with_matching_score()
```

### ตัวอย่าง Log หลังแก้ไข

โรคใบด่างมันสำปะหลัง:
```
🐛 โรคมีพาหะ 'แมลงหวี่ขาว' → ยังแนะนำยาฆ่าแมลงได้ (แม้จะ match skip keyword: ใบด่าง)
```

โรคใบด่างทั่วไป (ไม่มีพาหะ):
```
⏭️ Skipping product recommendation - matched skip keyword: ใบด่าง
```
